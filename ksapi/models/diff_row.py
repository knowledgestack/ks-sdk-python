# coding: utf-8

"""
    Knowledge Stack API

    Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user's live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema's `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from ksapi.models.diff_row_type import DiffRowType
from ksapi.models.diff_span import DiffSpan
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class DiffRow(BaseModel):
    """
    One aligned side-by-side row.  ``left`` is the old version, ``right`` is the new. For ``equal`` both are present; ``delete`` has left only; ``insert`` has right only; ``replace`` has both plus ``left_spans`` / ``right_spans`` marking the changed words. Line numbers are 1-based and null on the absent side.
    """ # noqa: E501
    type: DiffRowType
    left_no: Optional[StrictInt] = None
    left: Optional[StrictStr] = None
    right_no: Optional[StrictInt] = None
    right: Optional[StrictStr] = None
    left_spans: Optional[List[DiffSpan]] = None
    right_spans: Optional[List[DiffSpan]] = None
    __properties: ClassVar[List[str]] = ["type", "left_no", "left", "right_no", "right", "left_spans", "right_spans"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DiffRow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in left_spans (list)
        _items = []
        if self.left_spans:
            for _item_left_spans in self.left_spans:
                if _item_left_spans:
                    _items.append(_item_left_spans.to_dict())
            _dict['left_spans'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in right_spans (list)
        _items = []
        if self.right_spans:
            for _item_right_spans in self.right_spans:
                if _item_right_spans:
                    _items.append(_item_right_spans.to_dict())
            _dict['right_spans'] = _items
        # set to None if left_no (nullable) is None
        # and model_fields_set contains the field
        if self.left_no is None and "left_no" in self.model_fields_set:
            _dict['left_no'] = None

        # set to None if left (nullable) is None
        # and model_fields_set contains the field
        if self.left is None and "left" in self.model_fields_set:
            _dict['left'] = None

        # set to None if right_no (nullable) is None
        # and model_fields_set contains the field
        if self.right_no is None and "right_no" in self.model_fields_set:
            _dict['right_no'] = None

        # set to None if right (nullable) is None
        # and model_fields_set contains the field
        if self.right is None and "right" in self.model_fields_set:
            _dict['right'] = None

        # set to None if left_spans (nullable) is None
        # and model_fields_set contains the field
        if self.left_spans is None and "left_spans" in self.model_fields_set:
            _dict['left_spans'] = None

        # set to None if right_spans (nullable) is None
        # and model_fields_set contains the field
        if self.right_spans is None and "right_spans" in self.model_fields_set:
            _dict['right_spans'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DiffRow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "left_no": obj.get("left_no"),
            "left": obj.get("left"),
            "right_no": obj.get("right_no"),
            "right": obj.get("right"),
            "left_spans": [DiffSpan.from_dict(_item) for _item in obj["left_spans"]] if obj.get("left_spans") is not None else None,
            "right_spans": [DiffSpan.from_dict(_item) for _item in obj["right_spans"]] if obj.get("right_spans") is not None else None
        })
        return _obj


