# coding: utf-8

"""
    Knowledge Stack API

    Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user's live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema's `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List
from ksapi.models.metered_quota_status import MeteredQuotaStatus
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class KbSummaryResponse(BaseModel):
    """
    Point-in-time knowledge-base totals for a tenant.
    """ # noqa: E501
    documents_total: StrictInt
    documents_by_origin: Dict[str, StrictInt] = Field(description="Live document count keyed by document_origin.")
    documents_by_type: Dict[str, StrictInt] = Field(description="Live document count keyed by document_type.")
    document_versions_total: StrictInt
    chunks_total: StrictInt
    chunks_by_type: Dict[str, StrictInt]
    tokens_total: StrictInt = Field(description="Sum of chunk.num_tokens (cl100k).")
    sections_total: StrictInt
    threads_total: StrictInt
    messages_total: StrictInt
    messages_by_role: Dict[str, StrictInt]
    data_sources_total: StrictInt
    data_source_tables_total: StrictInt
    documents_checked_out: StrictInt
    quota: List[MeteredQuotaStatus] = Field(description="Current-period usage vs limit per metered resource.")
    feedback_up: StrictInt
    feedback_down: StrictInt
    __properties: ClassVar[List[str]] = ["documents_total", "documents_by_origin", "documents_by_type", "document_versions_total", "chunks_total", "chunks_by_type", "tokens_total", "sections_total", "threads_total", "messages_total", "messages_by_role", "data_sources_total", "data_source_tables_total", "documents_checked_out", "quota", "feedback_up", "feedback_down"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of KbSummaryResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in quota (list)
        _items = []
        if self.quota:
            for _item_quota in self.quota:
                if _item_quota:
                    _items.append(_item_quota.to_dict())
            _dict['quota'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of KbSummaryResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "documents_total": obj.get("documents_total"),
            "documents_by_origin": obj.get("documents_by_origin"),
            "documents_by_type": obj.get("documents_by_type"),
            "document_versions_total": obj.get("document_versions_total"),
            "chunks_total": obj.get("chunks_total"),
            "chunks_by_type": obj.get("chunks_by_type"),
            "tokens_total": obj.get("tokens_total"),
            "sections_total": obj.get("sections_total"),
            "threads_total": obj.get("threads_total"),
            "messages_total": obj.get("messages_total"),
            "messages_by_role": obj.get("messages_by_role"),
            "data_sources_total": obj.get("data_sources_total"),
            "data_source_tables_total": obj.get("data_source_tables_total"),
            "documents_checked_out": obj.get("documents_checked_out"),
            "quota": [MeteredQuotaStatus.from_dict(_item) for _item in obj["quota"]] if obj.get("quota") is not None else None,
            "feedback_up": obj.get("feedback_up"),
            "feedback_down": obj.get("feedback_down")
        })
        return _obj


