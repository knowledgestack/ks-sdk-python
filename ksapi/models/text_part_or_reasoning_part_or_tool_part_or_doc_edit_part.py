# coding: utf-8

"""
    Knowledge Stack API

    Knowledge Stack backend API for authentication and knowledge management

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from ksapi.models.doc_edit_part import DocEditPart
from ksapi.models.reasoning_part import ReasoningPart
from ksapi.models.text_part import TextPart
from ksapi.models.tool_part import ToolPart
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

TEXTPARTORREASONINGPARTORTOOLPARTORDOCEDITPART_ONE_OF_SCHEMAS = ["DocEditPart", "ReasoningPart", "TextPart", "ToolPart"]

class TextPartOrReasoningPartOrToolPartOrDocEditPart(BaseModel):
    """
    TextPartOrReasoningPartOrToolPartOrDocEditPart
    """
    # data type: TextPart
    oneof_schema_1_validator: Optional[TextPart] = None
    # data type: ReasoningPart
    oneof_schema_2_validator: Optional[ReasoningPart] = None
    # data type: ToolPart
    oneof_schema_3_validator: Optional[ToolPart] = None
    # data type: DocEditPart
    oneof_schema_4_validator: Optional[DocEditPart] = None
    actual_instance: Optional[Union[DocEditPart, ReasoningPart, TextPart, ToolPart]] = None
    one_of_schemas: Set[str] = { "DocEditPart", "ReasoningPart", "TextPart", "ToolPart" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = TextPartOrReasoningPartOrToolPartOrDocEditPart.model_construct()
        error_messages = []
        match = 0
        # validate data type: TextPart
        if not isinstance(v, TextPart):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TextPart`")
        else:
            match += 1
        # validate data type: ReasoningPart
        if not isinstance(v, ReasoningPart):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ReasoningPart`")
        else:
            match += 1
        # validate data type: ToolPart
        if not isinstance(v, ToolPart):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ToolPart`")
        else:
            match += 1
        # validate data type: DocEditPart
        if not isinstance(v, DocEditPart):
            error_messages.append(f"Error! Input type `{type(v)}` is not `DocEditPart`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in TextPartOrReasoningPartOrToolPartOrDocEditPart with oneOf schemas: DocEditPart, ReasoningPart, TextPart, ToolPart. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in TextPartOrReasoningPartOrToolPartOrDocEditPart with oneOf schemas: DocEditPart, ReasoningPart, TextPart, ToolPart. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into TextPart
        try:
            instance.actual_instance = TextPart.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ReasoningPart
        try:
            instance.actual_instance = ReasoningPart.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ToolPart
        try:
            instance.actual_instance = ToolPart.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into DocEditPart
        try:
            instance.actual_instance = DocEditPart.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into TextPartOrReasoningPartOrToolPartOrDocEditPart with oneOf schemas: DocEditPart, ReasoningPart, TextPart, ToolPart. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into TextPartOrReasoningPartOrToolPartOrDocEditPart with oneOf schemas: DocEditPart, ReasoningPart, TextPart, ToolPart. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], DocEditPart, ReasoningPart, TextPart, ToolPart]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


